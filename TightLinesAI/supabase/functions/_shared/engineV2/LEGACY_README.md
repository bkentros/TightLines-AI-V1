# Legacy engine V2

**Deprecated for How's Fishing.** The rebuild engine lives in `_shared/howFishingEngine/`.

This folder remains for **golden fixture tests** (`__tests__/goldenFixtures.test.ts`) until those are ported or retired.
