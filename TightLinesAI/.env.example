# Copy to .env and fill in (never commit .env).
# Expo / Supabase client (app)
# EXPO_PUBLIC_SUPABASE_URL=https://hsesngprhpgajyfbrwbf.supabase.co
# EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Auth email links: Safari often cannot open finfindr:// from Outlook/Mail.
# Host static/email-auth-redirect.html at this https URL and add the same URL
# in Supabase → Authentication → URL Configuration → Redirect URLs.
# EXPO_PUBLIC_AUTH_EMAIL_REDIRECT=https://finfindr.app/auth/confirm
# Optional override for password-reset emails only:
# EXPO_PUBLIC_AUTH_PASSWORD_EMAIL_REDIRECT=

# PostHog product analytics (client-safe public project key)
# EXPO_PUBLIC_POSTHOG_API_KEY=
# EXPO_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# OpenAI — local Deno scripts only (e.g. npm run gen:recommender-tackle-images).
# Do NOT prefix with EXPO_PUBLIC_; that would expose the key in the mobile bundle.
# OPENAI_API_KEY=

# Water Reader — local ops only (same values as Supabase Edge secrets where noted)
# Get project URL from the Supabase dashboard if needed.
# SUPABASE_URL=https://hsesngprhpgajyfbrwbf.supabase.co
# Must match the Edge Function secret (see Dashboard → Edge Functions → Secrets, or `supabase secrets list`).
# WATER_READER_INTERNAL_KEY=

# Instally creator install attribution (client-safe publishable key)
# EXPO_PUBLIC_INSTALLY_APP_ID=app_l5m5offjpseq
# EXPO_PUBLIC_INSTALLY_API_KEY=
# Configure the same value as RevenueCat's webhook Authorization header:
# Authorization: Bearer <REVENUECAT_WEBHOOK_AUTH_TOKEN>
# REVENUECAT_WEBHOOK_AUTH_TOKEN=
