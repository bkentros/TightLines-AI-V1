# Archived — pre-rebuild engine docs

These files described the **previous** V3 / multi-tab / window-based How’s Fishing system. They are **not** authoritative.

**Use instead:** `tlai_engine_rebuild_docs/` at the repository root.

Kept for history only. Do not implement new behavior from this folder.
