# Mirror of repo-root `tlai_engine_rebuild_docs/`

Canonical source-of-truth pack for the How's Fishing rebuild. Copied here so the **TightLinesAI/** tree contains the full spec pack for audits.
Edit the originals under `../../tlai_engine_rebuild_docs/` and re-copy if specs change.
